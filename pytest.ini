[pytest]
; Ensures `model` / `utils` are importable as top-level packages no matter
; how pytest is invoked (bare `pytest`, `python -m pytest`, an IDE runner,
; or CI), by putting this file's directory (ml-model/) on sys.path before
; test collection.
pythonpath = .
testpaths = tests
