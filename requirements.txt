fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.24
pillow>=10.0
python-multipart>=0.0.9
# torch is installed separately in the Dockerfile from the CPU-only wheel index
# (https://download.pytorch.org/whl/cpu) to avoid pulling ~2GB of unused CUDA deps.
# For local dev outside Docker: pip install torch>=2.0
